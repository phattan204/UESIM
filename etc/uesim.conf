# 5G UE Simulation Configuration File

# General settings
general {
    # Number of UE instances to simulate
    num_instances = 1;
    
    # Log level (0=ERROR, 1=WARN, 2=INFO, 3=DEBUG, 4=TRACE)
    log_level = 2;
    
    # Enable verbose logging
    verbose = false;
    
    # Enable debug mode
    debug = false;
}

# Network settings
network {
    # gNB IP address
    gnb_ip = "127.0.0.1";
    
    # gNB NGAP port
    gnb_ngap_port = 38412;
    
    # gNB GTP-U port
    gnb_gtpu_port = 2152;
    
    # Local bind address (0.0.0.0 for all interfaces)
    local_ip = "0.0.0.0";
    
    # Local ports (0 for random)
    local_ngap_port = 0;
    local_gtpu_port = 0;
}

# UE settings
ue {
    # IMSI range (format: MCC-MNC-MSIN)
    imsi_prefix = "00101";
    imsi_start = 1000000000;
    
    # MSISDN range
    msisdn_prefix = "12345";
    msisdn_start = 100000;
    
    # Tracking Area Code
    tac = 1;
    
    # Mobile Country Code
    mcc = "001";
    
    # Mobile Network Code
    mnc = "01";
}

# RRC settings
rrc {
    # RRC procedure timeouts (seconds)
    registration_timeout = 30;
    establishment_timeout = 30;
    reestablishment_timeout = 30;
    handover_timeout = 30;
    
    # Enable/disable specific procedures
    enable_registration = true;
    enable_establishment = true;
    enable_reestablishment = true;
    enable_handover = true;
}

# Performance settings
performance {
    # Thread pool size (0 for auto-detect)
    thread_pool_size = 0;
    
    # Buffer sizes
    rx_buffer_size = 65536;
    tx_buffer_size = 65536;
    
    # Memory pool size (bytes)
    memory_pool_size = 67108864;  # 64MB
    
    # Enable memory pool
    use_memory_pool = true;
}

# Security settings
security {
    # Enable stack protection
    stack_protection = true;
    
    # Enable ASLR
    aslr = true;
    
    # Enable secure compilation flags
    secure_flags = true;
}

# Test settings
test {
    # Enable unit tests
    enable_tests = false;
    
    # Test duration (seconds)
    test_duration = 60;
    
    # Test report file
    report_file = "/var/log/uesim/test_report.txt";
}